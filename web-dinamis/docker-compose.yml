# Layanan database

services:
  db-webdinamis:
    images: mariadb:lst
    environment:
      MARIADB_ROOT_PASSWORD: password!123
      MARIADB_DATABASE: dbcompro
      MARIADB_USER: userwebdinamis_2388010046
      MARIADB_PASSWORD: GwU0YsdRpzkmKLO[
    volumes:
      - db_data:/var/lib/mysql
      - ./db_data/dbcompro.sql:/docker-entrypoint-initdb.d/dbcompro.sql:ro
    restart: always

container-statis:
  image: ${DOCKERHUB_USERNAME}/compro_2388010046:latest
  ports:
      "80:80"
  restart: always

  container-dinamis:
      image: ${DOCKERHUB_USERNAME}/web-dinamis-2388010046:latest
      ports:
          "3000:3000"
      restart: always
      environment:
        DB_USER: userwebdinamis_2388010046
        DB_PASSWORD: GwU0YsdRpzkmKLO[
        DB_NAME: dbcompro
        DB_HOST: db-webdinamis
      depends_on:
          db-webdinamis

volumes:
  db_data: {}
